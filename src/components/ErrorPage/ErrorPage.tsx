const ErrorPage = ({ error }: any): JSX.Element => {
  return (
    <div>
      Something went wrong. We are already aware of the issue and are working on
      it.
    </div>
  );
};

export default ErrorPage;
